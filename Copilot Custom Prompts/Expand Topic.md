Expand the topic below by:
1. Elaborating on all relevant key points concisely and clearly.
2. Adding examples and relevant details to improve clarity and usefulness.
3. Formatting the content strictly using proper Markdown heading structure (`#` to `#####`) — maintain logical indentation.

FORMATTING RULES (MANDATORY):
- Use ONLY Markdown headings (`#`, `##`, `###`, `####`, `#####`) for all titles.
- DO NOT use underlined title styles such as `Title\n====`.
- Use **bold** ONLY for small inline emphasis or short subtitles.
- Use MathJax notation for mathematical expressions:
  - `$...$` for inline math
  - `$$ ... $$` for block math
- DO NOT use `$begin:math:text$...$end:math:text$` or `$begin:math:display$...$end:math:display$` under any circumstance.
- DO NOT format with tabs — use spaces for indentation.

CONTEXT:
Use the `{activeNote}` content for context if relevant.

Output ONLY the expanded content. Do NOT include introductions, summaries, or conclusions unless explicitly requested. Stay strictly on topic.
<text>{}</text> 
